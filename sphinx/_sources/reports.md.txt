# Test & Quality Reports

All reports are generated automatically by the CI/CD pipeline and published on GitHub Pages.

## Javadoc API Reference

Full Javadoc for all public classes and methods.

```{note}
**[→ Javadoc](../site/apidocs/index.html)**
```

## JaCoCo — Code Coverage

```{note}
**[→ JaCoCo Coverage Report](../site/jacoco/index.html)**
```

Minimum enforced: **25 % line coverage** (`mvn verify`).

## Surefire — Unit Test Report

```{note}
**[→ Surefire Unit Test Report](../site/surefire.html)**
```

> Note: the file is named `surefire.html` (not `surefire-report.html`).
> This is because the `report-only` goal is used, which skips re-running tests
> and only formats existing `surefire-reports/` XML files.

## Performance Tests

```{note}
**[→ Performance Report](../site/reports/perf-report.html)**
```

> The perf report is copied into `target/site/reports/` by the `performance`
> Maven profile, then picked up by `post-site` along with the rest of the Maven site.
> Published path: `site/reports/perf-report.html`.

## Checkstyle

```{note}
**[→ Checkstyle Report](../site/checkstyle.html)**
```

## PMD — Static Analysis

```{note}
**[→ PMD Report](../site/pmd.html)**

**[→ CPD (Copy-Paste Detector)](../site/cpd.html)**
```

## JDepend — Package Metrics

```{note}
**[→ JDepend Report](../site/jdepend-report.html)**
```

## Doxygen

```{note}
**[→ Doxygen HTML Reference](../doxygen/html/index.html)**
```

---

## Complete path map

| Report | Published URL path | Generated by |
|---|---|---|
| Javadoc | `site/apidocs/index.html` | `mvn site` (maven-javadoc-plugin in `<reporting>`) |
| JaCoCo | `site/jacoco/index.html` | `mvn test jacoco:report` |
| Surefire | `site/surefire.html` | `mvn site` (surefire-report-plugin `report-only`) |
| Performance | `site/reports/perf-report.html` | `mvn -Pperformance integration-test` + copy step |
| Checkstyle | `site/checkstyle.html` | `mvn site` |
| PMD | `site/pmd.html` | `mvn site` |
| CPD | `site/cpd.html` | `mvn site` |
| JDepend | `site/jdepend-report.html` | `mvn site` |
| Doxygen | `doxygen/html/index.html` | `mvn site` (doxygen-maven-plugin) |

---

## Generating All Reports Locally

```bash
# 1. Unit tests + JaCoCo
mvn test jacoco:report

# 2. Performance tests + copy report into site/reports/
mvn -Pperformance integration-test
mvn -Pperformance resources:copy-resources@copy-perf-report

# 3. Full Maven site (Javadoc, Checkstyle, PMD, JDepend, Surefire, JaCoCo, Doxygen)
mvn site

# 4. Copy everything to docs/
mvn post-site
```
